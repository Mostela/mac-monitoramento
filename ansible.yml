---
- name: "Monitoramento maquinas"
- all:
    hosts:
        # Ip Maquina
        vars: 
            ansible_python_interpreter: "/usr/bin/python"

  become: true
  become_method: sudo
  remote_user: joaocamargo812

  pre_tasks:
    - import_tasks: "./commons/pre_apt.yml"

  tasks:
    - import_tasks: "./roles/promethues.yml"
  post_tasks:
    - []
